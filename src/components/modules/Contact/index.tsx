import SectionHeading from '@/components/shared/SectionHeading';

const title = 'Get in touch';
const subtitle = 'Feel free to contact me anytime';

const Contact = () => {
  return (
    <div>
      <SectionHeading title={title} subtitle={subtitle} />
    </div>
  );
};

export default Contact;
